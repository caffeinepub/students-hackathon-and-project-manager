# Backend Gemini API Configuration
# IMPORTANT: This API key must be configured server-side only
# DO NOT add this to frontend environment variables (VITE_* prefix)
# The backend canister reads this from deployment secrets

# GEMINI_API_KEY=your_gemini_api_key_here

# Note: The actual key should be set during deployment via:
# - IC secrets management for production
# - Local .env file for development (backend only)
# - Never commit real API keys to version control
